services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=advanced_chat_db'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=advanced_chat_user'
    ports:
      - '5432'
  redis:
    image: 'redis:latest'
    ports:
      - '6379'
